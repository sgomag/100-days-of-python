# Day 24 – Mail Merge
A script that generates personalised letters by reading a guest list and a letter template, replacing the `[name]` placeholder with each guest's name, and saving a ready-to-send letter for each person.

This project practised navigating folder structures with relative file paths, and reading, editing and writing text files in Python.
