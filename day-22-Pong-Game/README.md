# Day 22 – Pong Game

A two-player Pong game built with Python's Turtle library.

In this project I further explored OOP, using multiple classes across multiple files, each with its own responsibility, and applying class inheritance.

Player to the left uses `w` and `s` to control the paddle. Player to the right uses `↑` and `↓`.